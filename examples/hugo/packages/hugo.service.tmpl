[Unit]
Description=Hugo {{ .Version }} static site generator ({{ .Arch }})
Documentation=https://gohugo.io/
After=network.target

[Service]
Type=simple
User=hugo
ExecStart=/usr/bin/hugo server --bind 0.0.0.0
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
